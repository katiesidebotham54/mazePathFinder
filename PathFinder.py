def man_dist(n1, n2):

	return n1[0] - n2[0] + n1[1] - n2[1] # (x1 - x2) + (y1 - y2)

def compute_path(maze): #1

	return

def repeated_forward(mazes):

	while mazes:

		maze = mazes[0]

		while maze: #14

			counter = 0 #15
			open_list = []
			closed_list = []
			search = np.zeros((len(maze),len(maze[0]))) #16 & 17

			while True:

				counter += 1

			compute_path(maze)

		mazes.pop(0)

		

	return None